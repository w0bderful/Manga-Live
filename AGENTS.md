# Manga Live 작업 지침

이 문서는 사용자가 대화에서 지정한 현재 유효한 요구사항과 프로젝트 작업 기준을 정리한다. 이후 사용자의 명시적인 변경·철회 지시가 있으면 해당 내용을 우선하고 이 문서도 맞춘다. 과거에 철회되거나 되돌린 기능을 임의로 다시 추가하지 않는다.

## 프로젝트와 소통

- 프로젝트 이름은 **Manga Live**다. Windows용 일본어 → 한국어 화면 번역기다.
- 사용자에게 작업 내용과 검증 결과를 한국어로 간결하게 설명한다.
- 기능 변경 시 현재 코드와 기존 작업 내용을 먼저 확인하고, 관련 없는 미커밋 변경은 보존한다.
- README는 실제 프로그램의 기능·설정·실행 방법과 일치하도록 유지한다. 프로그램과 README가 AI 창작물이라는 안내를 유지한다.

## 새 기능의 설정 저장

- 앞으로 추가하는 기능에 사용자가 선택하거나 조절하는 값이 있으면 **저장과 재실행 시 복원까지 함께 구현**한다.
- 설정 파일이 없을 때의 기본값, 잘못된 값, 파일 손상, 읽기·쓰기 실패를 처리한다. 저장 실패로 프로그램 전체가 종료되지 않도록 한다.
- 파일 저장은 임시 파일에 쓴 뒤 교체하는 방식으로 기존 정상 설정을 보호한다.
- 로컬 설정 파일과 실제로 생성하는 임시 파일을 `.gitignore`에 반영한다.
- 이 규칙만을 이유로 기존의 모든 비저장 설정을 한꺼번에 바꾸지는 않는다. 요청한 기능의 범위 안에서 적용한다.

## UI와 번역 동작

- 상단 메뉴바에는 **기본 모드**, **고급 모드**만 둔다. 제거한 메뉴 고정 기능이나 `설정창 열기` 메뉴 항목을 복원하지 않는다. 별도 설정창은 `Ctrl+,`로 연다.
- 기본 모드에서도 **영역 선택 · 드래그 번역 · 번역 시작 · 다시 번역**의 큰 버튼 4개를 메인 창에 유지한다.
- 기본 모드에서는 일반 설정을 별도 설정창에, 고급 모드에서는 메인 창에 표시한다. 모드 전환으로 선택값이나 진행 중인 번역을 불필요하게 초기화하지 않는다.
- 시작 버튼의 기본 이름은 **번역 시작**이다. 동작 상태에 따른 **일시정지**, **취소** 표시는 유지한다.
- **드래그 번역**은 드래그를 끝내면 선택 영역을 한 번 바로 번역한다. 별도의 시작 버튼 조작을 요구하지 않는다.
- 선택한 번역 영역은 바깥쪽 청록색 테두리로 표시한다. 완료·일시정지 후에도 유지하고 새 영역 선택·종료 시 정리한다. 테두리가 마우스 조작이나 원문 캡처를 방해하지 않도록 한다.
- 네 가지 실행 기능은 사용자 지정 전역 단축키를 지원하고 저장·복원한다.
- 시작 시 OCR 처리 장치는 **GPU 모드**를 기본 선택한다. CPU 모드 선택과 오류 안내도 유지한다.
- 프로그램 창의 **최상단 고정** 기능을 유지한다.
- 사용자가 삭제한 안내 문구를 다시 넣지 않는다. 특히 상단 소개 문구, 단축키 버튼 위의 긴 기능 나열, `번역 모드` 제목, OpenAI 호환 API 아래의 두 줄 안내 문구를 복원하지 않는다.
- Discord 관련 전용 기능과 안내는 추가하지 않는다.

## 번역 API

- API 키 입력칸은 사용자가 요청한 대로 문자를 가리지 않고 표시한다. 이것이 실제 키를 로그·답변·테스트 결과·Git에 노출해도 된다는 뜻은 아니다.
- 마지막으로 선택한 번역 API를 저장·복원한다.
- OpenAI 호환 API의 주소는 직접 변경할 수 있게 하고, 주소·키·선택한 모델을 저장한다. 모델은 서버 목록을 불러와 선택하며 직접 이름을 입력하는 방식으로 되돌리지 않는다.
- DeepL Free와 Pro는 하나의 선택 항목으로 제공하고 키에 맞는 서버를 선택한다.
- DeepL 사용량은 **남은 번역 가능 문자**만 표시한다. 수동 새로고침 버튼, 조회 시각, 갱신 주기 안내를 추가하지 않는다. 자동 갱신 주기는 **5분**이다.
- DeepL 잔여 한도는 기본 모드에서는 메인 창에, 고급 모드에서는 **DeepL API 키 입력칸 바로 아래**에 표시한다.

## 번역 텍스트 표시

- 세로로 긴 영역에서는 한국어를 **위에서 아래로**, 열은 **오른쪽에서 왼쪽으로** 배치한다. 가로 영역은 가로쓰기를 유지한다.
- 긴 번역문, 여러 열, 작은 영역에서 글자 잘림과 겹침을 확인한다.
- 현재 글자 안쪽 여백은 **4px**이며 작은 영역에서는 줄어든다. 철회된 글자 패딩 조절·저장 기능을 임의로 복원하지 않는다.
- 현재 텍스트 배경은 **번역 영역 전체를 채우는 흰색 사각형**이다. 글자 주변의 흰 테두리나 흐림 효과로 대체하지 않는다.
- 배경 불투명도는 **고급 모드에서 0~100%**로 조절한다. 0%는 배경 없음, 100%는 완전 불투명이다. 글자 자체의 진하기는 유지한다.
- 배경 설정은 즉시 화면에 반영하고 `overlay-settings.json`에 저장·복원한다. 기본 모드에서는 조절 항목을 숨기되 적용값은 유지한다.
- 캡처한 원본 이미지를 불투명한 레이어로 다시 덮어 반투명 효과가 사라지지 않도록 한다. 실제 Windows 화면과 원문 재캡처에서 확인한다.

## 검증

- 프로젝트의 `.venv` Python으로 필요한 검증을 수행한다. 예: `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`.
- 변경 범위에 맞는 검증을 선택한다. 텍스트만 삭제하는 작은 UI 변경에 불필요한 테스트를 새로 만들지 않는다.
- 표시·캡처 변경은 모의 테스트뿐 아니라 실제 Windows 창에서도 확인한다. 코드가 수정돼도 이미 실행 중인 프로그램에는 자동 반영되지 않는 점을 고려한다.
- **번역이 잘 되는지 검증할 때는 사용자가 제공한 `tests/fixtures/mokuro-demo.png`를 기존 테스트 이미지와 함께 사용한다.** 세로쓰기, 여러 말풍선, 작은 글자, 효과음, 한국어 배치를 확인한다. 상세 안내는 `tests/README.md`를 참고한다.
- 해당 이미지가 없는 환경에서는 사용한 이미지와 검증 범위를 명확히 보고한다. 그 이미지로 검증했다고 주장하지 않는다.
- OCR만 실행하거나 번역 API를 모의 처리한 결과를 실제 번역 품질 검증으로 보고하지 않는다. CPU/GPU, OCR 방식, 번역 서비스, 실제 API 호출 여부를 구분한다.
- 일반 회귀 테스트는 가짜 API 키와 모의 응답을 사용한다. 테스트가 사용자의 저장된 API 설정을 덮어쓰거나 의도치 않은 실제 번역 요청을 보내지 않도록 한다.
- 재사용할 수동 검증 스크립트는 `tests/manual/`, 테스트 이미지는 `tests/fixtures/`에 둔다. `.venv`에 임시 검증 코드를 쌓지 않는다.

## Git과 파일 관리

- `.gitignore`에는 **주석을 넣지 않는다**. 초기에 사용자가 지정했다가 철회한 `.gitignore` 규칙을 다시 강제하지 않는다. 현재 실제 생성 파일과 최신 지시에 맞춰 관리한다.
- **`/tests/`는 Git 추적에서 제외**한다. 테스트 코드·이미지·결과를 강제로 추가하지 않는다.
- API 키, 개인 설정, 로그, 가상환경, 다운로드 모델을 커밋하지 않는다.
- 불필요한 파일을 정리할 때 코드 참조와 사용 여부를 확인한다. 가상환경·모델·현재 개인 설정을 임의로 삭제하지 않는다.
- 되돌리기 요청은 해당 기능과 관련 문서·테스트에 한정해 처리한다. 관계없는 변경까지 전체 초기화하지 않는다.
- 푸시 요청이 있으면 대상 변경사항을 검토하고 필요한 검증을 마친 뒤 커밋·푸시한다. 원격 변경을 확인하고 강제 푸시 없이 반영한다.
- 푸시 완료 여부와 미푸시 상태를 정확히 알린다. 파일 수정만 요청한 작업을 자동으로 푸시하지 않는다.

## 설치·실행 파일

- `setup.bat`은 가상환경 생성·의존성 설치 후 실행, `run.bat`은 기존 가상환경에서 실행하는 용도다.
- `run.bat`의 CMD 창은 최소화 상태로 시작한다.
- BAT 자체의 설치·실행 실패 안내는 한국어로 제공한다.
